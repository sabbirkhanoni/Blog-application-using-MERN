# Server
PORT=5000

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=blog_applicationDB

# express-session secret (used to sign the session cookie)
SESSION_SECRET=change-this-secret-in-production

CLIENT_ORIGIN=http://localhost:5173
